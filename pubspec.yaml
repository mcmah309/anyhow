name: anyhow
description: Idiomatic error handling capabilities to make your code safer, more maintainable, and errors
  easier to debug. Dart implementation of Rust's Result monad type and "anyhow" crate.
version: 1.1.0
repository: https://github.com/mcmah309/anyhow

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:

dev_dependencies:
  lints: ^3.0.0
  test: any
